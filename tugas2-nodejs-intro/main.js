const Modul = require('./latihan-3');
const prompt = require('prompt-sync')()

let x = parseInt(prompt('input your number 1 : '));
let y = parseInt(prompt('input your number 2 : '));

Modul.Sum_number(x, y);